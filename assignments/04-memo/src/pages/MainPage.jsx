import Form from "../components/Form";

function MainPage() {
  return (
    <div
      style={{
        width: "100%",
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Form />
    </div>
  );
}

export default MainPage;
